#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:searty) :silent t)
  )

(defpackage :ros.script.searty-index.3857544737
  (:use :cl))
(in-package :ros.script.searty-index.3857544737)

(defun main (&rest argv)
  (declare (ignorable argv))
  (destructuring-bind (system-name dist-dir database-file) argv
    (searty::index-system system-name dist-dir database-file)))

;;; vim: set ft=lisp lisp:
