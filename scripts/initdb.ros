#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '(:searty) :silent t)
  )

(defpackage :ros.script.initdb.3858681886
  (:use :cl))
(in-package :ros.script.initdb.3858681886)

(defun main (&rest argv)
  (declare (ignorable argv))
  (searty::connect-database)
  (searty::delete-all-records))
;;; vim: set ft=lisp lisp:
